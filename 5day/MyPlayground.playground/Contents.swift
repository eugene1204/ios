import UIKit

// 문제: 가장 가까운 편의점 찾기

// 맨 처음 시작시... 코드
let store1 = (x:3, y:5, name: "gs")
let store2 = (x:4, y:6, name: "seven")
let store3 = (x:1, y:7, name: "cu")

// 거리구하는 함수
//func distance(current: (x: Int, y: Int), target: )
